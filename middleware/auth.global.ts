export default defineNuxtRouteMiddleware((to, from) => {
    // // ** Data
    // const path = ['/dang-nhap', '/dang-ky']

    // // ** State
    // const isLoggedIn = useState('isLoggedIn')

    // if (isLoggedIn.value && path.includes(to.path)) {
    //     return navigateTo('/')
    // }
})
